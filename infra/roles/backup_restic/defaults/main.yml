---
# Версия restic пинуется и ставится бинарником с GitHub, а не из apt:
# в Ubuntu 24.04 пакет заметно отстаёт, а работа с S3-совместимым бэкендом —
# именно та область, где свежие исправления имеют значение.
restic_version: "0.19.1"
restic_bin: /usr/local/bin/restic
