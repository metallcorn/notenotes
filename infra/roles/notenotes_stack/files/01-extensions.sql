-- Управляется Ansible (roles/notenotes_stack).
--
-- Выполняется ТОЛЬКО при первичной инициализации кластера (когда каталог
-- данных пуст). Для уже существующей базы те же расширения досоздаёт
-- отдельный таск роли stack — иначе добавление расширения позже потребовало бы
-- пересоздания базы.

-- Векторный поиск (ТЗ §3.2: HNSW-индекс на items.embedding).
CREATE EXTENSION IF NOT EXISTS vector;

-- Нечёткий поиск по подстроке для «быстрых результатов» из ТЗ §10.
CREATE EXTENSION IF NOT EXISTS pg_trgm;

-- Поиск без учёта диакритики — база двуязычная.
CREATE EXTENSION IF NOT EXISTS unaccent;
